/**
 * The library version, mirrored in `package.json`.
 *
 * It is recorded in every trace header so a trajectory can be tied back to the model that
 * produced its predictions. A test keeps the two in sync.
 */
export const FOLDPOINT_VERSION = "0.1.0";
